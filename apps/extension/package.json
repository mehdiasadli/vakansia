{
  "name": "extension",
  "version": "0.0.0",
  "private": true,
  "description": "manifest.json description",
  "type": "module",
  "scripts": {
    "TODO: remove this to use dev": "wxt --port 5555",
    "TODO: remove this to use dev:firefox": "wxt -b firefox",
    "TODO: remove this to use build": "wxt build",
    "TODO: remove this to use build:firefox": "wxt build -b firefox",
    "TODO: remove this to use zip": "wxt zip",
    "TODO: remove this to use zip:firefox": "wxt zip -b firefox",
    "TODO: remove this to use compile": "tsc --noEmit",
    "TODO: remove this to use postinstall": "wxt prepare"
  },
  "dependencies": {
    "react": "catalog:react",
    "react-dom": "catalog:react"
  },
  "devDependencies": {
    "@types/react": "catalog:react",
    "@types/react-dom": "catalog:react",
    "@wxt-dev/module-react": "catalog:wxt",
    "typescript": "catalog:",
    "wxt": "catalog:wxt"
  }
}
